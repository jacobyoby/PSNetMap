# User-generated files (create these in YOUR workspace, NOT in module root)
*.drawio
*-inventory.json
*-network*.json
*.drawio.bkp
*-meta.json
scanmeta.json

# Claude Code settings
.claude/

# Test files (only in tests/ or repo root — not module source)
/test-*.ps1
tests/test-*.ps1

# OS files
.DS_Store
Thumbs.db

# IDE files
.vscode/
.idea/
*.swp
*.swo

# Output directories
output/
scans/

# Secrets
*.key
*.secret
credentials.json

# Build artifacts
build/
dist/
*.nupkg

# PowerShell module cache
PSGetModuleInfo.xml

# Code review artifacts / logs (never commit)
CODEX_REVIEW.txt
*-review.txt
*review*.log
review-*.md

# Claude Code: track the shared settings and hooks; personal state stays local.
!.claude/
.claude/*
!.claude/settings.json
!.claude/hooks/
